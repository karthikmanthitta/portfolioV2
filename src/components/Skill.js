const Skill = ({ title }) => {
  return (
    <div className="bg-light rounded-xl">
      <p className="text-dark font-bold p-2 text-sm md:text-md">{title}</p>
    </div>
  );
};

export default Skill;
